#ifndef SEARCH_ALGOS_H
#define SEARCH_ALGOS_H

/*standard library */
#include <stdio.h>

/* prototypes */
int linear_search(int *array, size_t size, int value);
int binary_search(int *array, size_t size, int value);

#endif /* search_algos header file */
