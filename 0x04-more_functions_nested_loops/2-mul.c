#include <stdio.h>

/**
 * mul - multiplies two numbers
 * @a: the first of the two numbers
 * @b: the second of the two numbers
 * Return: the result of the multiplication
 */

int mul(int a, int b)
{
	return (a * b);
}
